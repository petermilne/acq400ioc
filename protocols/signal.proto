Terminator = LF;
ExtraInput = Ignore;

setSig {
	out "\$1=%(A)d,%(B)d,%(C)d";
}

getSig {
	out "\$1";
	in "\$1=%d,%*d,%*d";
	@replytimeout {
		disconnect;
# connect happens automatically?
#		connect;    
	}			
}

getSigDx {
	in "\$1=%*d,%d,%*d";
}

getSigSense {
	in "\$1=%*d,%*d,%d";
}
