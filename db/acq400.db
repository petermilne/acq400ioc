record (stringin, "${UUT}:${SITE}:MODEL") {
	field(DTYP, "stream")
	field(INP, "@acq42x.proto getModel ${SPORT}")
	field(PINI, "YES")	
}

record (stringin, "${UUT}:${SITE}:PART_NUM") {
	field(DTYP, "stream")
	field(INP, "@acq42x.proto getPart ${SPORT}")
	field(PINI, "YES")
}

record (longin, "${UUT}:${SITE}:NCHAN") {
	field(DTYP, "stream")
	field(INP, "@acq42x.proto getNchan ${SPORT}")
	field(PINI, "YES")	
}

record (stringin, "${UUT}:${SITE}:SERIAL") {
	field(DTYP, "stream")
	field(INP, "@acq42x.proto getSerial ${SPORT}")
	field(PINI, "YES")	
}

record (stringin, "${UUT}:${SITE}:ROLE") {
	field(DTYP, "stream")
	field(INP, "@acq42x.proto getRole ${SPORT}")
	field(PINI, "YES")	
}

record (mbbo, "${UUT}:0:STACK_480") {
    field(DTYP, "Soft Channel")
    field(NOBT, "3")
    field(ZRST, "none")
    field(PINI, "YES")
}

